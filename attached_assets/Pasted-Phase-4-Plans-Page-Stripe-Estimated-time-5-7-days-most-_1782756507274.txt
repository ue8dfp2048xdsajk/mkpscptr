Phase 4 — Plans Page + Stripe
Estimated time: 5–7 days (most complex phase)
What you're doing:
• Build the Plans page (FREE / STARTER / PRO, monthly/annual/lifetime tabs)
• Create Stripe Products and Prices in the Stripe dashboard (6 prices: Starter monthly, Starter annual, Starter lifetime, Pro monthly, Pro annual, Pro lifetime)
• Create a Vercel API route: POST /api/checkout — creates a Stripe Checkout Session for the chosen plan, returns the checkout URL
• Redirect user to Stripe's hosted checkout page
• Create a Vercel API route: POST /api/webhooks/stripe — handles checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_failed
• On successful payment: update user's plan in MongoDB, send receipt via Resend
• Stripe webhook signature verification: stripe.webhooks.constructEvent(rawBody, sig, secret) — do not skip this
What to be aware of:
• Test mode first, always. Stripe has test card numbers (4242 4242 4242 4242) — use them. Do not switch to live mode until every flow works perfectly in test mode.
• Raw body on the webhook route — add export const config = { api: { bodyParser: false } } at the top of your webhook API file. Without this Vercel parses the body and Stripe's signature check fails.
• Idempotency — your webhook handler must check if you've already processed an event before acting on it. Store processed Stripe event IDs in MongoDB (processedEvents collection with TTL). Without this, a retry from Stripe could double-upgrade someone.
• The Plans page is just HTML and CSS — build it as a static page first with hardcoded prices, then wire up the checkout API call. Don't try to do both at once.
• Lifetime plans in Stripe are one-time payments (payment_intent), not subscriptions. Handle the payment_intent.succeeded webhook separately from customer.subscription.*.
Done when: Test user can pick a plan, go through Stripe checkout with test card, land on Dashboard with correct plan badge, and your MongoDB correctly reflects their plan.
Step 1 — Create your Stripe account and products
13. Go to stripe.com → create account (stays in Test mode by default — leave it there)
14. Dashboard → Products → Add product
15. Create two products: Starter and Pro
16. For each product, add up to 3 prices:
• Monthly (recurring)
• Annual (recurring)
• Lifetime (one-time payment)
17. Copy each price_... ID — you'll need all 6 (or fewer if you're launching with fewer tiers)

Starter monthly - $25/mo - ID: price_1TncKkCVFUSsDoVeK0J9rZIg
Starter annual - $199/yr ($17/mo, save 34%) price_1TncQYCVFUSsDoVeYVKEZbT3
Starter one time - $499/yr (pay once use forever) price_1TncQYCVFUSsDoVegAnhNf6t

Pro monthly $39/mo - ID: price_1TncRVCVFUSsDoVepQwPYP1L
Pro annual - $299/yr ($25/mo, save 36%) ID: price_1TncTDCVFUSsDoVeLmcdvJ9p
Pro one time - $799 - ID: price_1TncTDCVFUSsDoVeC4jqBBLD

Step 2 — Add env vars to Vercel
Go to Vercel → your project → Settings → Environment Variables. Add:

STRIPE_SECRET_KEY              sk_test_...        (Stripe Dashboard → Developers → API keys)
STRIPE_WEBHOOK_SECRET          whsec_...          (added in Step 3 below)
STRIPE_PRICE_STARTER_MONTHLY   price_...
STRIPE_PRICE_STARTER_ANNUAL    price_...
STRIPE_PRICE_STARTER_LIFETIME  price_...
STRIPE_PRICE_PRO_MONTHLY       price_...
STRIPE_PRICE_PRO_ANNUAL        price_...
STRIPE_PRICE_PRO_LIFETIME      price_...
BASE_URL                       https://your-app.vercel.app
BASE_URL must be your exact Vercel domain — the webhook handler calls set-plan internally using it.

Step 3 — Register the webhook in Stripe
18. Stripe Dashboard → Developers → Webhooks → Add endpoint
19. URL: https://your-app.vercel.app/api/webhooks/stripe
20. Events to listen for: checkout.session.completed (that's the only one the handler processes)
21. Click Add endpoint → copy the whsec_... signing secret → paste into Vercel as STRIPE_WEBHOOK_SECRET
22. Redeploy Vercel after adding env vars

Step 4 — Build the pricing UI
This is the main thing to build. A page or modal with:
• FREE / STARTER / PRO columns
• Monthly / Annual / Lifetime tabs
• An Upgrade button per plan that calls:

_startCheckout('pro', 'monthly')   // or 'starter', 'annual', etc.
_startCheckout is already in app.js — it hits /api/checkout, gets a Stripe URL back, and redirects the user. You just need the UI that calls it.
Build the HTML/CSS layout first with hardcoded prices, then wire the buttons. Don't try to do both at once.

Step 5 — Test the full flow
Use Stripe test card 4242 4242 4242 4242, any future expiry, any CVC.
Full happy path to verify:
23. Sign in → click Upgrade → select plan → Stripe checkout page opens
24. Pay with test card → redirected back to app with ?payment=success
25. App polls Clerk for up to 44 seconds waiting for webhook
26. Plan badge updates, upgrade toast appears
27. Check Clerk dashboard → user's publicMetadata.plan is updated
28. Check Stripe dashboard → webhook delivery shows 200 response

Two things to add before going live (not needed for test mode)
Idempotency — the original spec flags this. Stripe can retry a webhook if your server is slow to respond. Currently there's no guard against processing the same checkout.session.completed twice. For test mode this is fine. Before switching to live keys, add a check using Upstash Redis (already in the stack for rate limiting) to store processed Stripe event IDs with a 24-hour TTL.
getAuth() on /api/checkout — currently trusts the clerkUserId from the request body. Before live keys, verify the caller's actual Clerk session token on the server side.

One-line summary of what you're actually building in Phase 4
Stripe account + 6 prices → env vars in Vercel → webhook registered → pricing UI with Upgrade buttons → test the full loop
Ready to start when you are. Build the pricing UI first — come back when you want help wiring the buttons or testing the flow.