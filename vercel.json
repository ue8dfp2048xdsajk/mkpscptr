{
  "version": 2,
  "buildCommand": "node scripts/check-env.js",
  "installCommand": "npm install --prefix api",
  "outputDirectory": ".",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
